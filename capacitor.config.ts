import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'react-page-transition',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
